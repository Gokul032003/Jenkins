pipeline {
    agent any
    
    stages {
        stage('Build') {
            steps {
                // Compile Java code
                sh 'javac Main.java Rectangle.java'
            }
        }
        stage('Run') {
            steps {
                // Run Java program
                sh 'java Main'
            }
        }
    }
}
